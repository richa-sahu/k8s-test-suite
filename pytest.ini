[pytest]
testpaths = tests
addopts = --alluredir=allure-results -v
markers =
    cluster: cluster health tests
    deployment: deployment validation tests
    connectivity: service connectivity tests
    sonobuoy: sonobuoy conformance tests